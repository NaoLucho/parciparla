{% if carousel is defined and carousel is not null %}
  {% set items = carousel %}
{% endif %}

<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
<div id="myCarosusel" class="carousel slide" data-ride="carousel">
  <!-- Indicators -->
  <ol class="carousel-indicators">
  {% if items is defined %}
    {% for item in items %}
      <li data-target="#myCarousel" data-slide-to="{{loop.index0}}" {% if loop.first %}class="active"{% endif %}>
      </li>
      {% endfor %}
    {% endif %}
    
  </ol>

  <!-- Wrapper for slides -->
  <div class="carousel-inner">
  {% if items is defined %}
    {% for item in items %}
      <div class="item {% if loop.first %}active{% endif %}" >
        
        <img class ="w-100" src="{{ vich_uploader_asset(item, 'imageFile') }}" alt="ee">
      </div>
    {% endfor %}
  {% endif %}
  </div>

  <!-- Left and right controls -->
  <a class="left carousel-control" href="#myCarousel" data-slide="prev">
    <span class="glyphicon glyphicon-chevron-left"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="right carousel-control" href="#myCarousel" data-slide="next">
    <span class="glyphicon glyphicon-chevron-right"></span>
    <span class="sr-only">Next</span>
  </a>
</div>
{#
<div class="page-hero" id="homeCarousel">
  <div class="owl-carousel">AAAA
    {% if items is defined %}BBB
    {% for item in items %}CCC
    <div class="item" style="background: url({{ vich_uploader_asset(item, 'imageFile') }});">
      <div class="container">
      <div class="carousel-caption hidden-xs">
        <h3>{{ item.title }}</h3>
        <p>{{ item.content }}</p>
      </div>
      </div>
    </div>
    {% endfor %}
    {% endif %}
  </div>


   <div class="page-hero__title">
    <div class="container">
      <h1 class="text-center">Site builder</h1>
    </div>
  </div> 
</div>#}